# 开发环境

# 调用方式 => import.meta.env.MODE

VITE_APP_TITLE = '官方'
# 接口请求地址，会设置到 axios 的 baseURL 参数上
BASE_URL = https://localhost:8080/
VITE_APP_API_BASEURL = /test
# 调试工具，可设置 eruda 或 vconsole，如果不需要开启则留空
VITE_APP_DEBUG_TOOL = vconsole


# 是否开启代理
VITE_OPEN_PROXY0 = true